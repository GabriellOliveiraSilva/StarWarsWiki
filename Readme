1 - Inicia a criação do app
>> npx create-expo-app nome-do-seu-app --template blank

2 - Container do Docker para hospedar o servidor que faz as traduções
docker run -d  --name libretranslate  -p 5000:5000  libretranslate/libretranslate --load-only en,pt